repro:vm-bullseye:
  extends: .components_repro
  needs:
    - prep:sources
    - build:vm-bullseye
  variables:
    DISTS_VM: bullseye
    USE_QUBES_REPO_VERSION: "4.1"
    USE_QUBES_REPO_TESTING: "1"