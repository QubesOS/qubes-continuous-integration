devel:install:vm-fc42:
  extends: .devel_components_install
  needs:
    - devel:prep:sources
    - devel:build:vm-fc42

devel:install:vm-fc43:
  extends: .devel_components_install
  needs:
    - devel:prep:sources
    - devel:build:vm-fc43

devel:install:vm-fc44:
  extends: .devel_components_install
  needs:
    - devel:prep:sources
    - devel:build:vm-fc44
