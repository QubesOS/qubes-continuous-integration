devel:build:vm-fc43:
  extends: .devel_components_build
  needs:
    - devel:prep:sources

devel:build:vm-fc44:
  extends: .devel_components_build
  needs:
    - devel:prep:sources

devel:build:vm-fc45:
  extends: .devel_components_build
  needs:
    - devel:prep:sources
