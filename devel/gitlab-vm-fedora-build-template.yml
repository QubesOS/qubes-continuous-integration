devel:build:fedora-42:
  extends: .devel_templates_build
  needs:
    - devel:prep:sources

devel:build:fedora-42-minimal:
  extends: .devel_templates_build
  needs:
    - devel:prep:sources

devel:build:fedora-43:
  extends: .devel_templates_build
  needs:
    - devel:prep:sources

devel:build:fedora-43-minimal:
  extends: .devel_templates_build
  needs:
    - devel:prep:sources

devel:build:fedora-44-gnome:
  extends: .devel_templates_build
  needs:
    - devel:prep:sources

devel:build:fedora-44-minimal:
  extends: .devel_templates_build
  needs:
    - devel:prep:sources
