devel:install:vm-bookworm:
  extends: .devel_components_install
  needs:
    - devel:prep:sources
    - devel:build:vm-bookworm

devel:install:vm-trixie:
  extends: .devel_components_install
  needs:
    - devel:prep:sources
    - devel:build:vm-trixie
