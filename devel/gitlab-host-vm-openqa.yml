devel:publish:repo:
  extends: .devel_openqa_repo
  needs:
    - devel:prep:sources
    - devel:build:host-fc41
    - devel:build:vm-bookworm
    - devel:build:vm-trixie
    - devel:build:vm-fc42
    - devel:build:vm-fc43
    - devel:build:vm-fc44
#    - devel:build:vm-archlinux
